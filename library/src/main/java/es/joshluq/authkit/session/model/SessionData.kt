package es.joshluq.authkit.session.model

/**
 * Marker interface for additional session data.
 * Models used for storing session information must implement this interface.
 * It is highly recommended that classes implementing this interface are annotated with @Serializable.
 */
interface SessionData
